1 ConverterUtils 引用类型和基本类型转换的问题
2 最大最小函数类型的问题
3 事务模式下，更新，删除，新增包含批量的主键问题(跟事务没关系),这里是因为实体类设置了自增主键，但是实际上数据表没有设置自增导致，应该给一个友好的异常
     generatedKeys.getObject(RETURN_GENERATED_KEYS, tableInfoPrimaryKey.getField().getType());
     Method threw 'java.sql.SQLException' exception. SQLState：S1000 Illegal operation on empty result set.
4 多数据源启动，兼容非动态SQL数据源的问题
5 @RefreshScope JdkDynamicAopProxy获取真实数据源对象
6 Error SQL: delete from `t_team_role_data_permission` where `role_id` = 23 and `permission_type` in (ALL,DEPT)
    批量查询枚举的问题